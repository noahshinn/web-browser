# Starter code for Go projects

To copy into your project:

```bash
git clone https://github.com/noahshinn/go-starter.git && cd go-starter && cp -r `ls -A | grep -v .git` ../ && cd .. && rm -rf go-starter
```
